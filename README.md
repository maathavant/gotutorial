# Go Proxy Server

This is a simple reverse proxy server implemented in Go. It forwards all requests to example.com. You can modify the target URL in main.go to point to your desired destination.

## Usage

1. Run the server: `go run main.go`
2. Access the proxy at http://localhost:8080